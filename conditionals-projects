<!DOCTYPE HTML PUBLIC "-//W3C//DTD HTML 4.01 Transitional//EN" "http://www.w3.org/TR/html4/loose.dtd">
<html>

<head>
    <title>p5js Template</title>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/p5.js/0.5.3/p5.js"></script>
    
    <script>
    var x, y;
    var time;
    x="white"
        function setup() {
            createCanvas(800,800);
            background(0)
            
             //changes the color to yellow if left arrow key is pressed.
       
        }
        function draw() {
        //presents the text at the to of the screen.
        textSize(30);
        text("Press the arrow keys to change color!", 10, 30);
        text("Click and drag your mouse to draw!", 10, 70);
     if (keyCode === LEFT_ARROW) {
        x= "yellow"
        }
        //changes the color to red if the right arrow key is pressed.
        if (keyCode === RIGHT_ARROW) {
        x="red"}
        //changes the color to blue if the up arrow key is pressed.
        if (keyCode === UP_ARROW) {
        x="blue"
        }
        //changes the color to green if the down arrow key is pressed.
        if (keyCode === DOWN_ARROW) {
        x="green"
        }
        
        
        }
        
        function mouseDragged() {
        //fills the ellipse with the color that you have pressed.
        fill(x)
        //changes the stroke to the color you have pressed.
        stroke(x);
        //draws the ellipse.
        ellipse(mouseX,mouseY,50,50)
        
        function keyPressed(){
            if (keyCode === "w" || "W") {
        rect(mouseX,mouseY,30,30);
            }
        }
        
        

 



        }
    </script>
</head>

<body>
    <div id="code" class="container-fluid">
        <script src="https://gist-it.appspot.com/github/phillipc0735/p5js/blob/gh-pages/conditionals-project.html?footer=0"></script>
    </div>


    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.0/jquery.min.js"></script>
    <script>
        $(window).bind("load", function() {
            $('#code').before($('#defaultCanvas0').remove());
        });
        $(document).keydown(function(e) {
  var key = e.which;
  if($.inArray(key,ar) > -1) {
    e.preventDefault();
  }
  return true;
});

    </script>
</body>


</html>

